# Vendored anti-slop plugin

Vendored from [dmmulroy/anti-slop](https://github.com/dmmulroy/anti-slop) at commit
`6d538555cb151d4121ed51a27db81890eacf8ae9`.

The generic plugin is enabled in `oxlint.config.ts`. The copied Effect plugin is intentionally not enabled because this
project does not directly depend on Effect.
