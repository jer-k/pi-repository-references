import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Register the Repository References extension with Pi. */
export default function repositoryReferences(_pi: ExtensionAPI): void {
  // Extension behavior will be registered here.
}
